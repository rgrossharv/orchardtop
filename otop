#!/bin/sh

# Compatibility entry point: OrchardTop can be invoked as `otop`.
set -eu

script_dir=$(CDPATH= cd -- "$(dirname -- "$0")" && pwd)
if [ -x "$script_dir/bin/orchardtop" ]; then
	 exec "$script_dir/bin/orchardtop" "$@"
else
	 exec "$script_dir/orchardtop" "$@"
fi
